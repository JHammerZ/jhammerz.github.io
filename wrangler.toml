
# Should look like:
# [[kv_namespaces]]
# id = "NEW_ID_FROM_STEP_1"

# 3. Fix deploy workflow - remove bad --compatibility-date flag
cat > .github/workflows/deploy-cloudflare.yml <<'YAML'
name: "Cloudflare: Sovereign Content Edge Mesh"
on:
  workflow_dispatch:
env:
  NPM_CONFIG_LEGACY_PEER_DEPS: "true"
jobs:
  Cloudflare-Ancyst-CDN-Routing-Deployment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci --legacy-peer-deps --force || npm install --legacy-peer-deps --force
      - name: 2. Broadcast Static Assets to Low-Latency Corridors
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx -y wrangler@latest deploy
      - run: echo "GOD LOCKED Node 22 workers-types 5 kept"
YAML

compatibility-date flag - Node22 keep workers5 GOD LOCKED" git push origin main gh workflow run deploy-cloudflare.yml --ref 
mainname = "jhammerz-router" main = "worker.js" compatibility_date = "2024-09-12" compatibility_flags = ["nodejs_compat"]

[vars]
ENVIRONMENT = "production"
SOVEREIGN_HID = "JHammerZ-001"
GEO_RANK = "ONE_OF_ONE"
PROTOCOL = "Twenty_47"

[[kv_namespaces]]
binding = "AGENT_STATE_LEDGER"
id = "106ec5c22ff44bdea0e5a4b3049efb86"

[[kv_namespaces]]
id = "a3bf737976e147df9cc6e9bbdf5886a1"
