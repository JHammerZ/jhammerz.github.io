name: old-security
on: { workflow_dispatch: {} }
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm audit || true
      - run: python3 -m pip install safety && safety check || true
