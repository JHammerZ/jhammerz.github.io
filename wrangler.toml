compatibility_date = "2026-08-26"
main = "dist/server.cjs"

[build]
command = "npm run build"

# DISABLED: Durable Objects causing deploy failures
# If a worker actually needs DOs, uncomment + export the class in that worker's entrypoint
# 

[env.production]
