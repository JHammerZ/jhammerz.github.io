
# HBS v1.2 / H-FID Standard / REC v7.2
# Copyright (c) 2026 Joshua Hamilton [J-HammerZ]
# Licensed under MIT License
# Sovereign Author: Joshua Hamilton
# Forensic Audit: H-FID-FOR-000001-AUDIT 100/100
# GEO_RANK: ONE_OF_ONE (Verified Authority)
# REACH_MULTIPLIER_3000: SONIC_VELOCITY +100ms
# SLSA Level: 3

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true





# M-Law Language / H-FID Standard / REC v7.2


# M-Law Language / H-FID Standard / REC v7.2


# M-Law Language / H-FID Standard / REC v7.2

name: "SEO: Force Search Engine Indexation"

on:
  schedule:
  workflow_dispatch:
  workflow_dispatch:
    inputs:
      target_url:
        description: "Specific URL to index"
        required: false
        type: string


permissions:
  contents: write

jobs:
  force-index:
    runs-on: ubuntu-latest
    steps:
        uses: actions/checkout@v4

        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

        run: |
          python -m pip install --upgrade pip
          pip install google-auth google-api-python-client requests jq

        run: |
          set -euo pipefail
          mkdir -p .hfid/indexing
          if [ -f "sitemap.xml" ]; then
             grep -oPm1 "(?<=<loc>)[^<]+" sitemap.xml > .hfid/indexing/urls.txt || echo "https://github.io" > .hfid/indexing/urls.txt
          else
             echo "https://github.io" > .hfid/indexing/urls.txt
          fi

        env:
          GOOGLE_API_KEY_DATA: ${{ secrets.GOOGLE_API_KEY }}
        run: |
          set -euo pipefail
          if [ -z "$GOOGLE_API_KEY_DATA" ]; then
             echo "::error::GOOGLE_API_KEY secret is blank or missing."
             exit 1
          fi
          echo "$GOOGLE_API_KEY_DATA" > .the-google-creds.json

          python - << 'PY_EOF'
          import os
          import json
          from google.oauth2 import service_account
          from googleapiclient.discovery import build

          try:
              with open('.the-google-creds.json', 'r') as f:
                  creds_data = json.load(f)
          except Exception as e:
              print(f"::error::JSON parse failure: {e}")
              raise

          try:
              credentials = service_account.Credentials.from_service_account_info(creds_data, scopes=['https://googleapis.com'])
              service = build('indexing', 'v3', credentials=credentials)
          except Exception as e:
              print(f"::error::Auth handshake failed: {e}")
              raise

          url_list = []
          if os.path.exists('.hfid/indexing/urls.txt'):
              with open('.hfid/indexing/urls.txt', 'r') as f:
                  url_list = [line.strip() for line in f if line.strip()]
          if not url_list:
              url_list = ['https://github.io']

          for target_url in url_list:
              print(f"Publishing indexing request for: {target_url}")
              body = {'url': target_url, 'type': 'URL_UPDATED'}
              try:
                  res = service.urlNotifications().publish(body=body).execute()
                  print(f"API Response: {res}")
              except Exception as e:
                  print(f"Notification error for {target_url}: {e}")
          PY_EOF
          rm -f .the-google-creds.json

        run: |
          echo "Syncing indexation maps to alternative search engine pipelines..."
