name: Build LLMs Full (Manual Only)
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: No-op - preserve manual llms.txt
        run: |
          echo "Workflow disabled from auto-run to preserve clean llms.txt audit"
          ls -lh llms.txt llms-full.txt
