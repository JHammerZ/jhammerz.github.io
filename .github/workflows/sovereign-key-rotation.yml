name: sovereign-key-rotation - restored
true:
  workflow_dispatch: {}
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - run: echo active
concurrency:
  group: sovereign-key-rotation
  cancel-in-progress: false
