name: HEO Sovereign Broadcast
on:
  schedule:
    - cron: '0 0 * * *'
  push:
    branches: [main]
    paths:
      - 'sovereign/**'
      - '.well-known/**'
      - 'hfid/**'
      - '.github/workflows/sovereign.yml'
  workflow_dispatch:

jobs:
  sovereign:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # keep your existing sovereign job steps here
      - run: echo "Add your existing sovereign steps back if this overwrites them"
