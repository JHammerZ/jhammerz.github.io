name: SEO and Link Validator
on: workflow_dispatch
concurrency:
  group: seo-link-validator
  cancel-in-progress: true
jobs:
  kill:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'disabled'
