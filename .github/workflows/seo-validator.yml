name: SEO and Link Validator
on: workflow_dispatch
  schedule:
    - cron: '0 0 * * *'
concurrency:
  group: seo-link-validator-override
  cancel-in-progress: true
jobs:
  kill:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Zombie SEO workflow neutralized'

