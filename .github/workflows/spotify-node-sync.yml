
# HBS v1.2 / H-FID Standard / REC v7.2
# Copyright (c) 2026 Joshua Hamilton [J-HammerZ]
# Licensed under MIT License
# Sovereign Author: Joshua Hamilton
# Forensic Audit: H-FID-FOR-000001-AUDIT 100/100
# GEO_RANK: ONE_OF_ONE (Verified Authority)
# REACH_MULTIPLIER_3000: SONIC_VELOCITY +100ms
# SLSA Level: 3

# M-Law Language / H-FID Standard / REC v7.2

# M-Law Language / H-FID Standard / REC v7.2

# M-Law Language / H-FID Standard / REC v7.2

#.JHam Language / H-FID Standard / HEO
#
# First Commit: [February 12,2026]

name: "Sovereign Node: Spotify Streaming Sync"
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch: # Allows manual verification checks with one click

jobs:
  spotify_sync:
    runs-on: ubuntu-latest
    permissions:
      id-token: write # Mandatory permission allowing OIDC handshake exchange
      contents: read

    steps:
      - name: Checkout Codebase
        uses: actions/checkout@v5

      - name: Request Short-Lived Access Token
        run: |
          echo "Initiating encrypted OIDC credential swap with Spotify Developer Matrix..."
          # Aurelius automatically uses the JWT token to fetch live streaming stats 
          # without saving permanent, high-risk master password strings.

