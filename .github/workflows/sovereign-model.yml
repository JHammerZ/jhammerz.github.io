name: sovereign-model.yml - restored
on:
  workflow_dispatch: null
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - run: echo active
concurrency:
  group: sovereign-model
  cancel-in-progress: false
