name: lysander_infinite_runner
'on':
- push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: '20'
    - run: npm ci || npm install || echo 'no deps'
    - run: echo 'Restored lysander_infinite_runner - REGEX matrix trap intact'
concurrency:
  group: lysander_infinite_runner
  cancel-in-progress: false
