{
  "type": "commonjs",
  "dependencies": {
    "@ardrive/turbo-sdk": "^1.42.0",
    "@irys/sdk": "^0.2.11",
    "@storacha/client": "^2.1.4",
    "ethers": "^5.8.0"
  },
  "scripts": {
    "build": "tsc --noEmit || echo 'Type check - preserving intent - no build required for H-FID registry files'",
    "validate": "tsc --noEmit --skipLibCheck"
  }
}