# ==============================================================================
#          LYSANDER NETWORK MATRIX // CLOUDFLARE PAGES EDGE INGRESS
#          DESIGN DEPTH: LEVEL 4 PRODUCTION // INFINITE MEDIA PIPES
# ==============================================================================
# name: "Cloudflare: Sovereign Content Edge Mesh"

on:
# concurrency:
# group: ${{ github.workflow }}-${{ github.ref }}
# cancel-in-progress: true
  push:
    branches: [ main, master ]
# paths:
      - 'public/**'
workflow_dispatch:

jobs:
  execute-matrix:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Codebase
        uses: actions/checkout@v4

      - name: Verify Environment Configuration
        run: |
          echo "Running verification track for: deploy-cloudflare.yml"
          if [ -f "package.json" ]; then
            if [ -f "yarn.lock" ]; then yarn install --immutable; else npm install; fi
          fi
