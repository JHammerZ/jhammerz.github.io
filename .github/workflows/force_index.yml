name: 🔱 Tier Zero Sovereign Indexing

on:
  push:
    branches: [main]
  workflow_dispatch: 

jobs:
  force-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source of Truth
        uses: actions/checkout@v4

      - name: Broadcast Truth to Google
        uses: robingenz/action-google-indexing@main
        with:
          # THE DIFFERENT MOVE: Swapping the domain for the direct homepage file
          # This often bypasses the trailing-slash 403 errors
          siteUrl: 'https://github.io'
          gcpServiceAccountKey: ${{ secrets.GOOGLE_INDEXING_API }}
