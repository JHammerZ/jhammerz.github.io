concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
name: commit_reset
on: workflow_dispatch
concurrency:
  group: commit-reset-override
  cancel-in-progress: true
jobs:
  kill:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Zombie commit_reset workflow neutralized'

