name: "Security: H-FID Restitution Monitor"

on:
  push:
    branches: [ main ]
  workflow_dispatch:
  schedule:
    - cron: '0 */12 * * *'

permissions:
  contents: read
  id-token: write
  attestations: write
  issues: write

#... rest of your existing workflow...
