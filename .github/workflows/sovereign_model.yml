name: sovereign_model.yml - restored
on:
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - run: echo active
concurrency:
  group: sovereign_model
  cancel-in-progress: false
