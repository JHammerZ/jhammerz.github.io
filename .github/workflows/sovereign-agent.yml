name: sovereign-agent - restored
true:
  workflow_dispatch: {}
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - run: echo active
concurrency:
  group: sovereign-agent
  cancel-in-progress: false
