{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "build": "vite build && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs",
    "start": "node dist/server.cjs",
    "preview": "vite preview",
    "clean": "rm -rf dist server.js",
    "lint": "tsc --noEmit",
    "test": "echo 'Lysander 3.1 hybrid - no tests yet'",
    "sovereign:fix": "node scripts/sovereign-patch.js",
    "orchestrator": "tsx scripts/aurelius_orchestrator.ts",
    "validate-socials": "python3 -c \"import json, sys; d=json.load(open(\\\"socials-manifest.json\\\")); sys.exit(0) if \\\"platforms\\\" in d else sys.exit(1)\"",
    "check-mutations": "python3 watch-workspace.py",
    "cache-clean": "bash clean-matrix-cache.sh",
    "test-matrix": "npm run validate-power && npm run validate-io && npm run validate-cpu && npm run validate-links && npm run validate-memory && npm run validate-docs && npm run validate-rotator && npm run validate-schema && npm run validate-ignore && npm run validate-css && npm run validate-socket && npm run validate-pgp && npm run validate-theme && npm run validate-binary && npm run validate-refactor && npm run validate-preflight && npm run validate-redirects && npm run validate-keys && npm run validate-keys && npm run validate-socials && npm run check-mutations",
    "validate-keys": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"jhammerz_pubkey_mobile.asc\\\").exists() or Path(\\\"public\\\").exists() else sys.exit(1)\"",
    "validate-redirects": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"_redirects\\\").exists() else sys.exit(1)\"",
    "validate-preflight": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"track-preflight-ping.py\\\").exists() else sys.exit(1)\"",
    "validate-refactor": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"clean-code-refactor.py\\\").exists() else sys.exit(1)\"",
    "validate-binary": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"verify-binary-headers.py\\\").exists() else sys.exit(1)\"",
    "validate-theme": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"sync-terminal-theme.py\\\").exists() else sys.exit(1)\"",
    "validate-pgp": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"audit-pgp-claims.py\\\").exists() else sys.exit(1)\"",
    "validate-socket": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"track-socket-logger.py\\\").exists() else sys.exit(1)\"",
    "validate-css": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"minify-css.py\\\").exists() else sys.exit(1)\"",
    "validate-ignore": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\".gitignore\\\").exists() else sys.exit(1)\"",
    "validate-schema": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"validate-playlist-schema.py\\\").exists() else sys.exit(1)\"",
    "validate-rotator": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"rotate-telemetry-logs.py\\\").exists() else sys.exit(1)\"",
    "validate-docs": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"generate-api-docs.py\\\").exists() else sys.exit(1)\"",
    "validate-memory": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"track-memory-allocation.py\\\").exists() else sys.exit(1)\"",
    "validate-links": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"track-dead-links.py\\\").exists() else sys.exit(1)\"",
    "validate-cpu": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"optimize-cpu-load.py\\\").exists() else sys.exit(1)\"",
    "validate-io": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"track-storage-io.py\\\").exists() else sys.exit(1)\"",
    "validate-power": "python3 -c \"from pathlib import Path; import sys; sys.exit(0) if Path(\\\"track-power-insulation.py\\\").exists() else sys.exit(1)\""
  },
  "dependencies": {
    "@google/genai": "^1.29.0",
    "@google/generative-ai": "^0.24.1",
    "@tailwindcss/vite": "^4.1.14",
    "@vitejs/plugin-react": "^5.0.4",
    "d3": "^7.9.0",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "glob": "^13.0.6",
    "lucide-react": "^0.546.0",
    "motion": "^12.23.24",
    "react": "^19.0.1",
    "react-dom": "^19.0.1",
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "@types/d3": "^7.4.3",
    "@types/express": "^4.17.21",
    "@types/node": "^22.14.0",
    "autoprefixer": "^10.4.21",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.1.14",
    "tsx": "^4.23.12",
    "typescript": "~5.8.2",
    "vite": "^6.2.3"
  }
}