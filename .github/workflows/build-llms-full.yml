name: build-llms-full - restored
on:
  workflow_dispatch: {}
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - run: echo active
concurrency:
  group: build-llms-full
  cancel-in-progress: false
