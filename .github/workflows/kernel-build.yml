name: "Build: H-FID Sovereign Kernel"
on: workflow_dispatch

permissions:
  contents: read
  id-token: write

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      
      - name: Install kernel build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential flex bison libssl-dev libelf-dev bc dwarves pahole xz-utils zstd kmod git
          
      - name: Fetch Core Kernel Tree via High-Availability Mirror
        run: |
          set -e
          echo "Cloning specific kernel release tree directly from GitHub mirrors..."
          # Cloning with depth 1 ensures an instant download by pulling only the target version tag
          git clone --depth 1 --branch v6.10 https://github.com linux-6.10
          test -d linux-6.10
          
      - name: Configure Linux Source Tree Natively
        run: |
          set -e
          cd linux-6.10
          make defconfig
          ./scripts/config --enable CONFIG_MODULES
          ./scripts/config --enable CONFIG_MODULE_UNLOAD
          make olddefconfig
          make -j$(nproc) modules_prepare
          
      - name: Inject and Compile Sovereign Engine Natively
        run: |
          set -e
          mkdir -p $GITHUB_WORKSPACE/hfid_module
          
          cat << 'INNER_EOF' > $GITHUB_WORKSPACE/hfid_module/Kernel_Override.c
          #include <linux/module.h>
          #include <linux/init.h>
          #include <linux/kernel.h>
          
          MODULE_LICENSE("GPL");
          MODULE_AUTHOR("JHammerZ");
          MODULE_DESCRIPTION("H-FID Sovereign Kernel Hooks");
          MODULE_VERSION("6.10-HFID-RING-3");
          
          static int __init hfid_init(void) {
              pr_info("H-FID: ONE_OF_ONE kernel active\n");
              pr_info("H-FID: GEO_RANK ONE_OF_ONE verified\n");
              pr_info("H-FID: RING_-3 sovereignty injected\n");
              return 0;
          }
          static void __exit hfid_exit(void) { pr_info("H-FID: Sovereign mode disengaged\n"); }
          module_init(hfid_init); module_exit(hfid_exit);
          INNER_EOF
          
          echo "obj-m += Kernel_Override.o" > $GITHUB_WORKSPACE/hfid_module/Kbuild
          
          echo "Compiling structural sovereign components..."
          make -C $GITHUB_WORKSPACE/linux-6.10 -j$(nproc) M=$GITHUB_WORKSPACE/hfid_module modules
          test -f $GITHUB_WORKSPACE/hfid_module/Kernel_Override.ko
          
      - uses: actions/upload-artifact@v4
        with:
          name: hfid-kernel-override
          path: hfid_module/Kernel_Override.ko
